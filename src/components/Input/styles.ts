import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    input: {
        padding: 15,
        marginTop: 10,
        width: "100%",
        borderWidth: 1,
        borderRadius: 10,
        borderColor: "white",
        color: "white",
        fontSize: 16
    }
}) 