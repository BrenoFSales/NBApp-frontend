import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    input: {
        padding: 15,
        marginTop: 20,
        width: "70%",
        borderWidth: 1,
        borderRadius: 10,
        borderColor: "white",
        color: "white",
        fontSize: 18
    }
}) 